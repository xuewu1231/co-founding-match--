一周MVP开发计划 - 适合独立小白开发者

     🎯 核心目标

     一句话: 用户能注册→填写Profile→看到别人的匿名卡片→点击"我想聊聊"→双向匹配后解锁真实身份    


     ---
     💡 技术栈极简推荐（小白友好）

     方案：Next.js全栈 + Supabase
     理由:
     - ✅ 无需配置后端服务器，Supabase提供开箱即用的数据库+认证
     - ✅ Next.js可以前后端一体化开发，学习成本低
     - ✅ 部署简单（Vercel一键部署）
     - ✅ 社区资源丰富，遇到问题容易找到答案

     具体技术:
     前端: Next.js 14 + TypeScript + Tailwind CSS
     后端: Supabase (PostgreSQL + Auth + Storage)
     部署: Vercel (免费)
     UI库: shadcn/ui (复制粘贴即用)
     表单: React Hook Form + Zod

     ---
     📦 MVP功能精简版（只保留核心）

     ✅ 保留的功能
     1. 简化注册: 邮箱+密码注册，跳过门户页
     2. Profile引导: 3步快速完善（基本信息→能力标签→寻找方向）
     3. 名片池: 展示匿名卡片 + 标签筛选
     4. 兴趣表达: "我想聊聊"按钮
     5. 双向匹配: 自动检测+解锁身份
     6. 已连接页: 查看已匹配的人

     ❌ 砍掉的功能（后续迭代）
     - 门户引流页面（直接进入注册）
     - 通知中心（用邮件通知代替）
     - 复杂的隐私控制（默认规则：匿名 vs 已连接）
     - 7天反馈问卷（手动跟进）
     - 头像上传（先用默认头像或gravatar）
     - 每日20次限制（暂不限制）

     ---
     📅 7天开发计划（每天任务清单）

     Day 1: 项目初始化 + 数据库设计

     目标: 搭建基础框架，创建数据库
     # 任务清单
     □ 创建Next.js项目（npx create-next-app）
     □ 安装shadcn/ui和必要依赖
     □ 创建Supabase项目（https://supabase.com）
     □ 设计数据库表结构（5张表：users, profiles, tags, interests, connections）
     □ 在Supabase中创建表和关系

     关键文件准备:
     - /docs/database-schema.sql - 数据库建表脚本
     - /lib/supabase.ts - Supabase客户端配置
     - /types/database.types.ts - TypeScript类型定义

     ---
     Day 2: 认证系统 + 基础布局

     目标: 用户能注册登录
     # 任务清单
     □ 配置Supabase Auth
     □ 创建注册页面（/register）
     □ 创建登录页面（/login）
     □ 设置认证中间件（保护路由）
     □ 创建基础Layout组件（顶部导航栏）

     关键文件:
     - /app/register/page.tsx
     - /app/login/page.tsx
     - /middleware.ts
     - /components/layout/Navbar.tsx

     ---
     Day 3: Profile引导流程（3步简化版）

     目标: 新用户完成Profile填写
     # 任务清单
     □ Step 1: 基本信息（姓名、Title、简介）
     □ Step 2: 我的能力标签（多选，预设10个标签）
     □ Step 3: 寻找的方向（技术/商业/产品）
     □ 进度条组件
     □ Profile完整度计算（必填项验证）
     □ 完成后跳转到名片池

     关键文件:
     - /app/onboarding/[step]/page.tsx
     - /components/forms/ProfileForm.tsx
     - /components/ui/TagSelector.tsx
     - /lib/profile-helpers.ts

     核心逻辑:
     // Profile完整度计算
     function calculateCompletion(profile) {
       const required = ['name', 'title', 'abilities', 'seeking_role'];
       const completed = required.filter(field => profile[field]).length;
       return (completed / required.length) * 100;
     }

     ---
     Day 4: 名片池 + 标签筛选

     目标: 用户能看到匿名卡片并筛选
     # 任务清单
     □ 名片池页面布局（/matching/pool）
     □ 匿名卡片组件（AnonymousCard）
     □ 标签筛选器（TagFilter）
     □ 从Supabase查询用户数据
     □ 实现标签筛选逻辑
     □ "我想聊聊"按钮交互

     关键文件:
     - /app/matching/pool/page.tsx
     - /components/cards/AnonymousCard.tsx
     - /components/filters/TagFilter.tsx
     - /app/api/pool/route.ts (API路由)

     核心查询:
     -- 根据标签筛选用户
     SELECT DISTINCT u.id, p.title, p.vision
     FROM users u
     JOIN profiles p ON u.id = p.user_id
     JOIN user_tags ut ON u.id = ut.user_id
     WHERE ut.tag_id IN (1,2,3)  -- 选中的标签IDs
     AND u.id != current_user_id;

     ---
     Day 5: 兴趣表达 + 双向匹配逻辑

     目标: 实现核心匹配算法
     # 任务清单
     □ "我想聊聊"API（/api/interest/send）
     □ 检查是否已发起兴趣
     □ 双向匹配检测逻辑
     □ 匹配成功自动创建连接
     □ 发送邮件通知（Supabase Edge Function或Resend）
     □ 前端状态更新（已发起/已匹配）

     关键文件:
     - /app/api/interest/send/route.ts
     - /app/api/interest/check/route.ts
     - /lib/matching-logic.ts

     核心逻辑:
     // 双向匹配检测
     async function handleInterestSignal(senderId, receiverId) {
       // 1. 记录兴趣
       await supabase.from('interests').insert({
         sender_id: senderId,
         receiver_id: receiverId,
         status: 'pending'
       });

       // 2. 检查反向兴趣
       const { data: reverse } = await supabase
         .from('interests')
         .select()
         .eq('sender_id', receiverId)
         .eq('receiver_id', senderId)
         .eq('status', 'pending')
         .single();

       // 3. 如果双向，建立连接
       if (reverse) {
         await supabase.from('connections').insert({
           user_a_id: Math.min(senderId, receiverId),
           user_b_id: Math.max(senderId, receiverId),
           status: 'active'
         });

         // 更新兴趣状态
         await supabase.from('interests')
           .update({ status: 'accepted' })
           .in('id', [reverse.id]);

         // 发送邮件通知
         await sendMatchNotification(senderId, receiverId);
       }
     }

     ---
     Day 6: 已连接页面 + Profile查看

     目标: 用户能查看已匹配的人
     # 任务清单
     □ 已连接列表页面（/matching/connections）
     □ 查询已连接用户
     □ 展示真实身份信息
     □ 点击查看完整Profile
     □ Profile详情页（/profile/[id]）
     □ 基于关系的权限控制（匿名 vs 实名）

     关键文件:
     - /app/matching/connections/page.tsx
     - /app/profile/[id]/page.tsx
     - /components/profile/ProfileView.tsx
     - /lib/privacy-control.ts

     隐私控制逻辑:
     // 根据关系返回不同数据
     async function getVisibleProfile(targetId, requesterId) {
       const profile = await getProfile(targetId);

       // 检查是否已连接
       const { data: connection } = await supabase
         .from('connections')
         .select()
         .or(`user_a_id.eq.${targetId},user_b_id.eq.${targetId}`)
         .or(`user_a_id.eq.${requesterId},user_b_id.eq.${requesterId}`)
         .single();

       if (connection) {
         // 已连接：返回真实信息
         return {
           name: profile.name,
           title: profile.title,
           bio: profile.bio,
           linkedin: profile.linkedin,
           // ...
         };
       } else {
         // 陌生人：返回匿名信息
         return {
           name: null,  // 隐藏姓名
           title: profile.title,
           bio: null,
           abilities: profile.abilities,
           vision: profile.vision,
           // ...
         };
       }
     }

     ---
     Day 7: 测试 + 优化 + 部署

     目标: 可用的MVP上线
     # 任务清单
     □ 端到端测试完整流程
     □ 修复发现的bug
     □ UI细节优化（响应式、加载状态）
     □ 错误处理（表单验证、API错误）
     □ Vercel部署
     □ 配置环境变量
     □ 邀请2-3个朋友测试

     部署检查清单:
     - Supabase环境变量配置正确
     - 数据库Row Level Security (RLS)启用
     - 邮件通知能正常发送
     - 移动端适配正常

     ---
     🗂️ MVP文件结构（精简版）

     co-founder-mvp/
     ├── app/
     │   ├── (auth)/
     │   │   ├── login/page.tsx
     │   │   └── register/page.tsx
     │   ├── onboarding/
     │   │   └── [step]/page.tsx           # 3步Profile引导
     │   ├── matching/
     │   │   ├── pool/page.tsx             # 名片池
     │   │   └── connections/page.tsx      # 已连接
     │   ├── profile/[id]/page.tsx         # 查看Profile
     │   └── api/
     │       ├── interest/send/route.ts    # 发起兴趣
     │       └── pool/route.ts             # 获取卡片
     ├── components/
     │   ├── cards/
     │   │   └── AnonymousCard.tsx
     │   ├── forms/
     │   │   └── ProfileForm.tsx
     │   ├── layout/
     │   │   └── Navbar.tsx
     │   └── ui/                           # shadcn/ui组件
     ├── lib/
     │   ├── supabase.ts                   # Supabase配置
     │   ├── matching-logic.ts             # 匹配算法
     │   ├── privacy-control.ts            # 隐私控制
     │   └── helpers.ts
     ├── types/
     │   └── database.types.ts
     ├── docs/
     │   ├── database-schema.sql           # 数据库设计
     │   ├── mvp-features.md               # 功能清单
     │   └── deployment.md                 # 部署指南
     └── .env.local                        # 环境变量

     ---
     📄 需要准备的关键文档

     1. 数据库设计 (docs/database-schema.sql)

     包含5张核心表的建表语句：
     - users (用户表)
     - profiles (资料表)
     - tags (标签表)
     - interests (兴趣信号表)
     - connections (连接表)

     2. API接口文档 (docs/api-endpoints.md)

     列出所有API端点、请求参数、响应格式

     3. 开发日志 (docs/dev-log.md)

     记录每天完成的功能和遇到的问题

     4. 部署指南 (docs/deployment.md)

     Vercel部署步骤和环境变量配置

     ---
     🎬 立即开始的步骤

     Step 1: 创建项目

     # 创建Next.js项目
     npx create-next-app@latest co-founder-mvp --typescript --tailwind --app

     # 安装必要依赖
     cd co-founder-mvp
     npm install @supabase/supabase-js @supabase/auth-helpers-nextjs
     npm install react-hook-form zod @hookform/resolvers
     npx shadcn-ui@latest init

     Step 2: 创建Supabase项目

     1. 访问 https://supabase.com
     2. 创建新项目
     3. 复制项目URL和API Key到 .env.local

     Step 3: 生成开发文档

     我将帮你创建：
     - 数据库建表脚本
     - 每日开发prompt（详细的开发指令）
     - 组件示例代码
     - 核心逻辑伪代码

     ---
     ⚡ 成功关键建议

     1. 先跑通流程，再优化UI: 第一遍用最简单的样式，确保逻辑正确
     2. 一天一个功能: 严格按照7天计划，不要跳跃开发
     3. 多用shadcn/ui组件: 复制粘贴即用，节省时间
     4. 遇到问题立即问AI: Claude/ChatGPT可以帮你解决90%的小白问题
     5. 每天提交代码: 用Git管理版本，避免改坏了回不去

     预估时间: 一周（每天6-8小时专注开发）

     准备好开始了吗？我可以立即为你生成：
     - Day 1的详细开发指令
     - 数据库建表脚本
     - 项目初始化配置文件
     - 第一个组件的示例代码

  ⎿